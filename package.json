{
  "name": "nyemil-njabrik",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack UMKM snack e-commerce and monitoring dashboard for Nyemil Njabrik.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "install:all": "npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "engines": {
    "node": ">=20"
  }
}
